# Supabase Edge Functions local environment

# Preferred provider: OpenAI ChatGPT
OPENAI_API_KEY=sk-REPLACE_ME
# Optional; defaults to gpt-4o-mini if not set
OPENAI_MODEL=gpt-4o-mini

# Fallback legacy gateway (optional)
_API_KEY=REPLACE_ME_OR_DELETE

# Supabase values are provided automatically in deployed environments.
# If you run functions locally with `supabase start`, these are injected.
# Only set these manually if you know you need to override for local dev:
# SUPABASE_URL=http://127.0.0.1:54321
# SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_LOCAL_SERVICE_ROLE_KEY